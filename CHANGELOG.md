# Changelog

本文件记录“嘉盛物料映射工具”的主要功能变化。

版本格式采用：

```text
主版本.次版本.修订版本
```

当前历史版本根据本轮开发过程整理。

---

## [1.0.0] - 2026-09-10

### Added

- 建立规范化项目目录：
  - `app/`
  - `data/`
  - `backup/`
  - `logs/`
- 新增统一入口 `启动工具.bat`。
- BAT 菜单支持：
  - `[1] 导入客户物料清单`
  - `[2] 合并外部映射汇总表`
  - `[Q] 退出`
- 新增 `app/customer_import.py`。
- 新增 `app/mapping_merge.py`。
- 新增主表自动备份机制。
- 新增外部映射表交互式冲突处理。
- 新增合并操作：
  - 覆盖
  - 新增
  - 跳过
  - 取消全部
  - 后续同类冲突批量处理
- 新增独立日志：
  - `logs/customer_import.log`
  - `logs/mapping_merge.log`

### Changed

- 主映射表规范命名为：
  - `data/嘉盛物料映射汇总表.xlsx`
- 客户文件处理目录规范化为：
  - `data/客户清单/待处理`
  - `data/客户清单/已处理`
  - `data/客户清单/异常`
- 外部映射合并目录规范化为：
  - `data/映射合并/待合并`
  - `data/映射合并/已合并`
- 程序文件改用职责型英文命名：
  - `customer_import.py`
  - `mapping_merge.py`
- Python 程序不再依赖固定电脑绝对路径，统一从项目根目录计算路径。
- 移除 Python 脚本内部的重复退出暂停，由 BAT 统一处理退出。

### Safety

- 修改已有主汇总表前自动创建完整备份。
- 合并外部映射时，“覆盖”默认仅作用于 `M:V` 人工映射区。
- 来源映射空白不会清除主表已有映射。
- 冲突时允许人工选择处理方式。
- 选择 `Q` 取消合并时，本次主表修改不保存，来源文件不移动。
- D 多重匹配等歧义场景不自动猜测。

---

## [0.3.0] - 2026-09-10

### Added

- 新增“合并别人维护过的映射汇总表”功能。
- 支持按 B 物料号优先匹配。
- B 为空时支持按 D 匹配。
- 支持 M:V 人工映射区域比较。
- 支持异常行由用户选择覆盖、新增或跳过。
- 支持批量应用后续冲突处理策略。

### Safety

- 覆盖时默认不修改 A:L 基础数据。
- 主表映射为空而来源有值时可安全补齐。
- 来源为空时不清除主表已有映射。

---

## [0.2.0] - 2026-09-10

### Added

- 客户清单导入前自动备份已有主汇总表。
- 新增 `backfill_material_no_by_d` 配置开关。
- 新增“历史空物料号回填”能力。

### Changed

物料去重/回填规则升级为：

```text
新数据B有物料号
    ↓
先按B查重
    ↓
B不存在
    ↓
按D查找历史B为空记录
    ↓
D唯一匹配 → 只补写历史行B
D无匹配   → 新增
D多重匹配 → 不自动回填
```

### Safety

- 回填物料号时只修改 B 列。
- M:V 人工维护映射保持不变。
- D 多重匹配时不自动覆盖。

---

## [0.1.0] - 2026-09-08

### Added

- 建立客户 Excel 批量汇总脚本。
- 支持同时读取：
  - `电柜元器件`
  - `电柜半成品`
- 支持两张 Sheet 不同列结构映射为统一结构。
- 支持 `.xls`、`.xlsx`、`.xlsm`。
- 支持多线程文件处理。
- 支持成功文件和异常文件分类移动。
- 支持处理日志。

### Changed

- 优化 Excel 读取性能：
  - 同一个工作簿只打开一次。
  - `.xlsx/.xlsm` 使用 `iter_rows(values_only=True)`。
  - `.xls` 使用 `xlrd` 按需打开。
  - 文件排序改用预构建索引，避免重复线性查找。
- 数据结束条件调整为 **D 列为空**。

### Data rules

- B 列作为主要物料去重字段。
- B 为空的新物料仍然提取。
- B 为空时使用 D 作为备用去重字段。

---

## 后续计划

尚未实现、可考虑的功能：

- 映射关系完整性检查。
- 重复映射/一对多关系专项检查。
- ERP 导入模板转换。
- 合并操作预览报告。
- 备份自动保留策略。
- 配置文件外置。
- GUI 操作界面。

