# CHANGELOG

本文件记录“嘉盛物料映射工具”的主要功能变化。

版本格式采用：`主版本.次版本.修订版本`。

---

## v1.5.1 - 2026-09-11

### Changed

- 脚本3生成的 `*_已匹配.xlsx` 打开时，默认激活 `京能物料对应汇总` Sheet，方便直接查看标准化映射结果。
- 仅调整工作簿默认活动 Sheet，不改变客户原始 Sheet、匹配结果、公共物料数量或脚本4接口。

---

## v1.5.0 - 2026-09-11

### Added

- 脚本3新增“报价单驱动公共物料数量”。
- 报价单位于项目根目录，文件名必须同时包含当前项目号（如 `JSA26125`）和“报价”，且必须唯一。
- 脚本3按电气清单文件名解析合并柜号范围，并在报价单同名项目 Sheet 中查找柜号集合完全一致的明细块。
- 公共物料以主映射表 `公共物料` Sheet 的 D 列型号匹配报价明细 C 列，并读取 F 列数量。
- 新增 Python 内置特殊规则：`ZQV2.5N/50` 在报价中匹配 `ZQV2.5N/10`，最终数量使用 `round(报价数量 / 5)`。该规则不写入 Excel 配置。
- 报价单按项目号缓存解析结果，同一项目多张电气清单只读取/解析一次对应报价 Sheet。

### Changed

- 公共物料数量从“固定映射数量”改为“报价数量驱动”；M:X 中公共物料映射数量继续作为单位换算系数，空白默认 1。
- 脚本3保持合并柜型粒度：例如 `JSA26125-11~13` 在回填阶段仍为一张清单、一套单台柜 BOM 数量，不乘柜子台数。
- 单台柜拆分仍由脚本4完成；同一脚本3结果可供 `JSA26125-11`、`-12`、`-13` 等 ET 分别使用。
- 脚本1/3扫描项目电气清单时排除文件名包含“报价”的 Excel，避免把报价单误当作客户清单处理。
- 脚本3日志新增每个电气清单命中的报价文件、报价柜号块、起始行、公共物料输出条数及异常数量。

### Safety

- 找不到报价单、报价单不唯一、缺少项目 Sheet、找不到完全对应的柜号块、柜号块重复、公共物料型号缺失/重复、报价 F 列数量异常或特殊换算失败时，脚本3停止自动流程。
- 上述异常会使脚本5停在脚本3阶段，不继续生成 ERP。

---

## v1.4.2 - 2026-09-10

### Changed

- 脚本4读取 `京能物料对应汇总` 时，若 `京能物料号` 为 `客供件`，直接过滤，不写入 ERP BOM。
- 客供件过滤发生在数量校验之前，因此客供件数量为空不会导致 ERP 阶段异常。
- 脚本4改为在每个 ET 母件内部按子件物料号合并数量。
- ST 子件与京能物料均参与合并；若两者子件号相同，同样合并为一行。
- 合并后保持子件首次出现顺序，并重新生成从 1 开始的连续顺序号。
- 保持 ST 默认数量为 1、物料导入 B 列权威范围及真实 `.xls` COM 输出规则不变。

---

## v1.4.1 - 2026-09-10

### Performance

- 优化脚本3：同一项目存在多个 `.xls` 时只启动一次 Excel COM，连续转换后统一退出，减少 Excel 启停开销。
- 优化脚本3：1:N 展开复制 A:Z 时直接复制 `_style`，去掉字体、填充、边框、对齐、保护等重复 deepcopy。
- 优化脚本3：缓存高频列号，客户清单结束行扫描改为按列迭代，减少循环中的重复计算和随机单元格访问。
- 脚本3日志新增单文件耗时和总耗时，方便后续继续定位性能瓶颈。
- 保持 v1.4.0 的匹配规则、数量规则、公共物料、输出目录和 ERP 流程不变。

---

## v1.4.0 - 2026-09-10

### 项目目录驱动

- 菜单1、3、4改为选择任意外部项目文件夹执行。
- 项目文件夹不再要求放入工具 `data` 目录。
- 脚本1、3不再移动或归档项目原始电气清单，原始资料保持不变。
- 脚本1、3自动递归扫描项目电气清单，并排除 `输出`、`物料导入*.xls` 和 `01-...嘉盛-导入.xlsx`。

### 项目输出归档

- 脚本3输出改为 `项目\输出\物料回填`。
- 脚本4输出改为 `项目\输出\ERP`。
- ERP异常报告改为 `项目\输出\异常`。
- 脚本1、3、4、5日志统一写入 `项目\输出\logs`。
- 工具根 `logs` 仅保留脚本2等全局任务日志。

### ERP模板目录

- `ERP物料清单批量导入表.xls` 从 `data` 移至 `templates`。
- 明确该文件只作为真实 Excel 97-2003 `.xls` 输出模板。

### Added

- 新增脚本5 `project_workflow.py`：项目一键处理流程编排器。
- 固定流程：脚本1 → 脚本3 → 脚本4。
- 脚本1发现新物料时立即停止，并说明原因。
- 脚本3存在任何匹配异常时立即停止，不执行脚本4。
- 只有脚本3全匹配时才自动生成 ERP `.xls`。
- 任一步硬错误都会停止，并将原因写入项目日志。

### 启动器

- 菜单新增 `5 - 项目一键处理（1 -> 3 -> 4）`。
- 菜单1、3、4、5使用 Windows 文件夹选择窗口选择项目路径，无需在 CMD 手工输入路径。
- 保留菜单2作为全局映射维护。

### Other

- 继续使用主映射 M:X 六组映射区域。
- 保留脚本3公共物料、统一汇总、长数字物料号文本化、1:N 展开规则。
- 保留脚本4 ET/ST、文件名范围展开、物料导入 B 列权威范围、真实 `.xls` COM 输出规则。

---

## v1.3.0 - 2026-09-10

### Added

- 新增脚本4 `erp_bom_export.py`：生成 ERP 物料清单批量导入表。
- 新增菜单 `4 - 生成 ERP 物料清单批量导入表`。
- 脚本4不再要求重复复制原始电气清单；只需提供 `物料导入*.xls`，直接复用脚本3输出。
- 新增目录：
  - `data/ERP导入/待处理`
  - `data/ERP导入/输出`
  - `data/ERP导入/异常`
- 内置真实 Excel 97-2003 模板：`data/ERP导入/ERP物料清单批量导入表.xls`。

### ERP BOM Rules

- `物料导入*.xls` A列中 ET 为母件。
- 从一个 ET 开始，后续所有 ST 属于该 ET，直到下一个 ET。
- ST 作为子件，当前数量固定 1。
- B列作为项目号，直接匹配脚本3 `_已匹配.xlsx` 文件名展开结果。
- 支持 `41~50` 等连续范围展开。
- 支持 `76~83(有高效)` 等带括号备注的连续范围展开。
- 以 `物料导入.xls` 为权威项目范围；电气清单中多出的订单不生成、不报错。
- 从脚本3输出的 `京能物料对应汇总` 读取京能物料号和京能数量作为 ET 子件。
- 本版本中京能物料重复出现时不合并。
- 每个 ET 的顺序号从 1 重新开始。
- ERP 最终输出为真正的 `.xls`，不是仅修改扩展名。

### Safety

- 任意 ET 匹配失败时，不生成部分 BOM。
- 文件名歧义、B列歧义、缺少脚本3结果、缺少汇总 Sheet、数量异常等都会输出异常报告。
- ERP 物料编码按文本写入，避免长数字科学计数法。

### Changed

- 主映射人工映射区统一扩展为 `M:X`，支持物料 A~F。
- 脚本3升级为公共物料 + 统一汇总 + 性能优化版本。
- 启动器增加 `pywin32` 依赖检查。
- 根 BAT 统一为纯 ASCII、CRLF、无 BOM 的 `run_tool.bat`。

---

## v1.2.0 - 2026-09-10

### Added

- 新增脚本3：生成客户清单京能物料对应表。
- 新增 `京能物料对应汇总` Sheet，统一汇总 `电柜元器件` 与 `电柜半成品` 的结果。
- 新增 AC 列匹配状态和异常高亮。
- 新增从主映射表 `公共物料` Sheet 自动追加公共物料。

### Changed

- 映射区扩展为 `M:X`，支持物料 A~F。
- 汇总表 I/J 列确定为 `京能物料号`、`京能数量`。
- 长纯数字京能物料号按文本写入，避免科学计数法。
- 支持客户物料 1:N 映射自动展开。
- 京能数量按“客户数量 × 映射数量”计算；映射数量为空默认 1，主映射表 G 列不参与计算。
- B 有值时只按 B 匹配；B 为空时才按 D 匹配。
- 客户原始文件保留，处理结果输出到独立目录。
- 中文菜单交由 `launcher.py`；BAT 仅作为纯 ASCII 一键启动入口。

### Performance

- 无待处理文件时立即结束，不加载主映射表。
- 主映射表改为 `read_only=True + iter_rows(values_only=True)` 流式扫描。

### Safety

- 未匹配、重复匹配、未配置京能物料、数量异常均保留状态并高亮，不静默猜测。
- 公共物料数量为空默认 1，且不乘客户数量。

---

## v1.1.0 - 2026-09-10

### Added

- 新增 `app/material_fillback.py`：根据主映射表向客户清单回填我方物料。
- 新增菜单 `3 - 生成客户清单物料对应表`。
- 新增 `data/物料回填/待处理、输出、已处理、异常` 目录。
- 新增 `logs/material_fillback.log`。
- 客户清单新增 `AA=嘉盛物料号`、`AB=嘉盛数量`、`AC=匹配状态`。
- 支持客户物料与我方物料 1:N 展开，自动插入缺少的行。
- 映射范围扩展到物料 F（M:X，共 6 组）。
- 支持异常行高亮和明确状态。
- `.xls` 可通过 Windows Excel COM 临时转换后处理，原始文件保持不变。

### Rules

- 客户 B 有值时严格按 B 匹配；B 为空时才按 D 型号匹配。
- 最终数量 = 客户原清单数量 × 映射单位数量。
- 映射数量为空默认 1；非空非法值标记异常。
- 主映射表 G 列数量不参与回填计算。
- B/D 多重匹配、未找到映射、未配置我方物料、数量异常均高亮。

### Changed

- BAT 任务结束菜单统一为 `M - 返回主菜单` / `Q - 退出工具`，避免方括号在部分 CMD 编码环境下解析异常。

---

## v1.0.0 - 2026-09-10

### Added

- 建立规范化项目目录：`app/`、`data/`、`backup/`、`logs/`。
- 新增统一入口 `启动工具.bat`。
- BAT 菜单支持：
  - `1 - 导入客户物料清单`
  - `2 - 合并外部映射汇总表`
  - `Q - 退出`
- 新增 `app/customer_import.py`。
- 新增 `app/mapping_merge.py`。
- 新增主表自动备份机制。
- 新增外部映射表交互式冲突处理。
- 新增合并操作：覆盖、新增、跳过、取消全部，以及后续同类冲突批量处理。
- 新增独立日志：`logs/customer_import.log`、`logs/mapping_merge.log`。

### Changed

- 每个任务执行结束后可选择 `M` 返回主菜单继续操作，或 `Q` 退出工具。
- 主映射表规范命名为 `data/嘉盛物料映射汇总表.xlsx`。
- 客户文件处理目录规范化为 `data/客户清单/待处理、已处理、异常`。
- 外部映射合并目录规范化为 `data/映射合并/待合并、已合并`。
- 程序文件改用职责型英文命名：`customer_import.py`、`mapping_merge.py`。
- Python 程序不再依赖固定电脑绝对路径，统一从项目根目录计算路径。
- 移除 Python 脚本内部重复退出暂停，由启动器统一处理退出。

### Safety

- 修改已有主汇总表前自动创建完整备份。
- 合并外部映射时，“覆盖”默认仅作用于人工映射区。
- 来源映射空白不会清除主表已有映射。
- 冲突时允许人工选择处理方式。
- 选择 `Q` 取消合并时，本次主表修改不保存，来源文件不移动。
- D 多重匹配等歧义场景不自动猜测。

---

## v0.3.0 - 2026-09-10

### Added

- 新增“合并别人维护过的映射汇总表”功能。
- 支持按 B 物料号优先匹配。
- B 为空时支持按 D 匹配。
- 支持 M:V 人工映射区域比较。
- 支持异常行由用户选择覆盖、新增或跳过。
- 支持批量应用后续冲突处理策略。

### Safety

- 覆盖时默认不修改 A:L 基础数据。
- 主表映射为空而来源有值时可安全补齐。
- 来源为空时不清除主表已有映射。

---

## v0.2.0 - 2026-09-10

### Added

- 客户清单导入前自动备份已有主汇总表。
- 新增 `backfill_material_no_by_d` 配置开关。
- 新增“历史空物料号回填”能力。

### Changed

物料去重/回填规则升级为：

```text
新数据 B 有物料号
    ↓
先按 B 查重
    ↓
B 不存在
    ↓
按 D 查找历史 B 为空记录
    ↓
D 唯一匹配 → 只补写历史行 B
D 无匹配   → 新增
D 多重匹配 → 不自动回填
```

### Safety

- 回填物料号时只修改 B 列。
- 人工维护映射区保持不变。
- D 多重匹配时不自动覆盖。

---

## v0.1.0 - 2026-09-08

### Added

- 建立客户 Excel 批量汇总脚本。
- 支持同时读取 `电柜元器件`、`电柜半成品`。
- 支持两张 Sheet 不同列结构映射为统一结构。
- 支持 `.xls`、`.xlsx`、`.xlsm`。
- 支持多线程文件处理。
- 支持成功文件和异常文件分类移动。
- 支持处理日志。

### Changed

- 优化 Excel 读取性能：
  - 同一个工作簿只打开一次。
  - `.xlsx/.xlsm` 使用 `iter_rows(values_only=True)`。
  - `.xls` 使用 `xlrd` 按需打开。
  - 文件排序改用预构建索引，避免重复线性查找。
- 数据结束条件调整为 D 列为空。

### Data Rules

- B 列作为主要物料去重字段。
- B 为空的新物料仍然提取。
- B 为空时使用 D 作为备用去重字段。
